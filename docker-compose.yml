version: "3.8"
services:
  backend:
    build: ./
    volumes:
      - .:/usr/src/app
    networks:
      - whitelabel
    ports:
      - 80:80

networks:
  whitelabel:

volumes:
  db:
    driver: local